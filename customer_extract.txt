
---function Customers---
function Customers(){const[rows,setRows]=useState<Customer[]>([]);const[orders,setOrders]=useState<Order[]>([]);const[name,setName]=useState('');const[selected,setSelected]=useState<string|null>(null);useEffect(()=>{Promise.all([customerService.list(),orderService.list()]).then(([customers,loadedOrders])=>{const data=customers.length?customers:seedNames.map((name,i)=>safeCustomer({id:`cust-${String(i+1).padStart(3,'0')}`,name,email:`${name.toLowerCase().replace(/ /g,'_')}@example.com`,phone:`+961 70 ${120000+i}`,address:`${10+i} Main Street`,city:cities[i%7],joinedDate:'2025-01-15'},i));setRows(data.map(safeCustomer));setOrders(loadedOrders.length?loadedOrders:seed())}).catch(()=>setOrders(seed()))},[]);const stats=(c:Customer)=>{const matches=orders.filter(o=>(o.customerId&&o.customerId===c.id)||(o.customerEmail&&o.customerEmail.toLowerCase()===c.email.toLowerCase())||(!o.customerId&&!o.customerEmail&&o.customer===c.name));const latest=[...matches].sort((a,b)=>new Date(b.date||b.updatedAt).getTime()-new Date(a.date||a.updatedAt).getTime())[0];return {matches,count:matches.length,total:matches.reduce((sum,o)=>sum+(Number(o.total)||0),0),last:latest?.date||'—'}};const add=async()=>{if(!name.trim())return;const c=safeCustomer({id:crypto.randomUUID(),name:name.trim(),fullName:name.trim(),city:'Beirut',joinedDate:new Date().toISOString().slice(0,10)},rows.length);await customerService.create(c);setRows(r=>[c,...r]);setName('')};const edit=async(c:Customer)=>{const n=prompt('Customer full name',c.fullName||c.name);if(n&&n!==c.fullName){await customerService.update(c.id,{name:n,fullName:n});setRows(r=>r.map(x=>x.id===c.id?{...x,name:n,fullName:n}:x))}};const remove=async(id:string)=>{if(confirm('Delete customer?')){await customerService.remove(id);setRows(r=>r.filter(x=>x.id!==id));if(selected===id)setSelected(null)}};return <div className="page"><div className="toolbar"><div><p className="muted">Customer profiles and order history</p><h2>Customers</h2></div><div className="search"><input placeholder="New customer name" value={name} onChange={e=>setName(e.target.value)}/><button className="btn primary" onClick={add}><Plus/>Add</button></div></div><div className="customergrid">{rows.map(c=>{const s=stats(c);const active=c.status!=='Inactive'&&s.count>0;return <div className="card customerCard" key={c.id}><div className="customerTop"><div className="bigavatar">{c.name.split(' ').map(x=>x[0]).join('').slice(0,2)}</div><div><button className="icon" aria-label={`Edit ${c.name}`} onClick={()=>edit(c)}><Edit3 size={15}/></button><button className="icon" aria-label={`Delete ${c.name}`} onClick={()=>remove(c.id)}><Trash2 size={15}/></button></div></div><h3>{c.fullName||c.name} <span className="count">{active?'Active':c.status}</span></h3><p>{c.email||'No email'} · {c.phone||'No phone'}</p><p>{c.address||'No address'}{c.area?`, ${c.area}`:''}, {c.city}</p><div className="customerMeta"><span>{s.count} orders</span><b>${s.total.toFixed(2)}</b><span>Last: {s.last}</span></div><button className="btn" onClick={()=>setSelected(selected===c.id?null:c.id)}>{selected===c.id?'Hide history':'View order history'}</button>{selected===c.id&&<div className="customerHistory">{s.matches.length?s.matches.map(order=><Link key={order.id} to={`/orders/${order.id}`}><b>{order.id}</b><span>{order.date||'—'} · ${(order.total||0).toFixed(2)}</span><Badge status={order.status}/></Link>):<small>No orders linked yet.</small>}</div>}</div>})}</div></div>}
/* 5: hna analytics page b7ot charts 3ashan insights w performance summary. */

---function App---
function App(){const[dark,setDark]=useState(()=>localStorage.getItem('routely-theme')==='dark'||(!localStorage.getItem('routely-theme')&&window.matchMedia('(prefers-color-scheme: dark)').matches));const[mobile,setMobile]=useState(false);const[collapsed,setCollapsed]=useState(()=>localStorage.getItem('routely-sidebar')==='collapsed');const[panel,setPanel]=useState<'search'|'notifications'|'profile'|null>(null);const[q,setQ]=useState('');const[orders,setOrders]=useState<Order[]>([]);const[user,setUser]=useState<AuthUser|null>(()=>authService.current());useEffect(()=>{localStorage.setItem('routely-theme',dark?'dark':'light');document.documentElement.classList.toggle('dark-mode',dark)},[dark]);useEffect(()=>{localStorage.setItem('routely-sidebar',collapsed?'collapsed':'expanded')},[collapsed]);useEffect(()=>{const close=(e:KeyboardEvent)=>{if(e.key==='Escape'){setMobile(false);setPanel(null)}};window.addEventListener('keydown',close);return()=>window.removeEventListener('keydown',close)},[]);useEffect(()=>{orderService.list().then(x=>setOrders(x.length?x:seed())).catch(()=>setOrders(seed()))},[]);if(!user)return <Routes><Route path="/register" element={<Register onLogin={setUser}/>}/><Route path="*" element={<Login onLogin={setUser}/>}/></Routes>;const logout=()=>{authService.logout();setUser(null)};const userName=typeof user.name==='string'&&user.name?user.name:typeof user.email==='string'&&user.email?user.email:'User';return <div className={dark?'app dark':'app'}><Sidebar user={user} mobile={mobile} collapsed={collapsed} close={()=>setMobile(false)}/>{mobile&&<button className="sidebarOverlay" aria-label="Close navigation" onClick={()=>setMobile(false)}/>}<main><header><button className="icon mobile" aria-label="Open navigation" onClick={()=>setMobile(true)}><Menu/></button><button className="icon desktopSidebarToggle" aria-label={collapsed?'Expand sidebar':'Collapse sidebar'} onClick={()=>setCollapsed(v=>!v)}>{collapsed?<Menu/>:<X/>}</button><div><div className="eyebrow">LEBANON • OPERATIONS</div><h1>{pageTitle()}</h1></div><div className="actions"><button aria-label="Search" className="icon" onClick={()=>setPanel(panel==='search'?null:'search')}><Search/></button><button aria-label="Notifications" className="icon" onClick={()=>setPanel(panel==='notifications'?null:'notifications')}><Bell/><i/></button><button aria-label="Toggle dark mode" className="icon" onClick={()=>setDark(!dark)}>{dark?<Sun/>:<Moon/>}</button><button className="avatar" onClick={()=>setPanel(panel==='profile'?null:'profile')}>{userName.split(' ').map(part=>part[0]).join('').slice(0,2)}</button></div>{panel==='search'&&<div className="headerPanel searchPanel"><div className="search"><Search size={16}/><input autoFocus placeholder="Search orders, customers, tracking..." value={q} onChange={e=>setQ(e.target.value)}/></div>{q&&orders.filter(o=>Object.values(o).join(' ').toLowerCase().includes(q.toLowerCase())).slice(0,5).map(o=><Link key={o.id} to={'/orders/'+o.id} onClick={()=>setPanel(null)}><b>{o.id}</b><span>{o.customer} · {o.trackingNumber}</span></Link>)}</div>}{panel==='notifications'&&<div className="headerPanel"><b>Notifications</b><p>3 shipments need attention</p><p>Driver Fadi is near Beirut</p><Link to="/tracking" onClick={()=>setPanel(null)}>Open tracking</Link></div>}{panel==='profile'&&<div className="headerPanel"><b>{userName}</b><p>{user.role} · {user.city}</p><Link to="/profile" onClick={()=>setPanel(null)}>My profile</Link><button className="btn" onClick={logout}>Log out</button></div>}</header><Routes><Route path="/" element={<Dashboard/>}/><Route path="/orders" element={<Orders/>}/><Route path="/orders/new" element={<OrderForm/>}/><Route path="/orders/:id/edit" element={<OrderForm/>}/><Route path="/orders/:id" element={<OrderDetails/>}/><Route path="/tracking" element={<Tracking/>}/><Route path="/customers" element={<Customers/>}/><Route path="/analytics" element={<Analytics/>}/><Route path="/settings" element={<SettingsPage/>}/><Route path="/profile" element={<ProfilePage user={user} onUpdate={setUser} onLogout={logout}/>}/><Route path="*" element={<Navigate to="/" replace/>}/></Routes></main></div>}
/* 2: hna pageTitle b7ot route label 3ashan UX ykoun clear. */

---function OrderForm---
function OrderForm(){const nav=useNavigate();const {id}=useParams();const editing=Boolean(id&&id!=='new');const[error,setError]=useState('');const[original,setOriginal]=useState<Order|null>(null);const[form,setForm]=useState<Partial<Order>>({customer:seedNames[0],customerId:'cust-001',city:'Beirut',address:'Hamra Street',phone:'+961 70 000000',total:50,status:'Processing',date:new Date().toISOString().slice(0,10),items:1,progress:20,trackingNumber:`LB${Date.now()}`,paymentMethod:'Cash on delivery',deliveryWindow:'10:00 - 14:00',notes:'',driver:'Unassigned',products:[{name:'Product',quantity:1,price:50}],createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()});useEffect(()=>{if(editing)orderService.list().then(x=>{const found=x.find(o=>o.id===id);if(found){setOriginal(found);setForm(found)}else setError('Order not found.')}).catch(()=>setError('Could not load this order.'))},[editing,id]);const set=(k:keyof Order,v:string)=>setForm(f=>({...f,[k]:k==='total'||k==='items'||k==='progress'?Number(v):k==='status'?safeStatus(v):v}));return <div className="page formpage"><div className="toolbar"><div><p className="muted">Orders / {editing?'Edit':'New'} order</p><h2>{editing?'Edit order':'Create new order'}</h2></div></div><form className="card form" onSubmit={async e=>{e.preventDefault();setError('');try{const selectedIndex=Math.max(0,seedNames.indexOf(form.customer||''));const payload={...form,customerId:form.customerId||`cust-${String(selectedIndex+1).padStart(3,'0')}`,status:safeStatus(form.status),...(editing&&original&&form.status!==original.status&&form.progress===original.progress?{progress:progressForStatus(safeStatus(form.status))}: {})};if(editing&&id)await orderService.update(id,payload);else await orderService.create({id:`ORD-${Date.now()}`,...payload} as Order);nav('/orders')}catch(err){setError(err instanceof Error?err.message:'Could not save order.')}}}><h3>Shipment details</h3>{error&&<p className="error">{error}</p>}<div className="formgrid"><label>Customer<select value={form.customer||''} onChange={e=>set('customer',e.target.value)}>{seedNames.map(n=><option key={n}>{n}</option>)}</select></label><label>Phone<input value={form.phone||''} onChange={e=>set('phone',e.target.value)}/></label><label className="full">Address<input value={form.address||''} onChange={e=>set('address',e.target.value)}/></label><label>City<input value={form.city||''} onChange={e=>set('city',e.target.value)}/></label><label>Status<select value={safeStatus(form.status)} onChange={e=>set('status',e.target.value)}>{statusOptions.map(status=><option key={status} value={status}>{status}</option>)}</select></label><label>Amount<input type="number" value={form.total||0} onChange={e=>set('total',e.target.value)}/></label><label>Tracking number<input value={form.trackingNumber||''} onChange={e=>set('trackingNumber',e.target.value)}/></label><label>Delivery window<input value={form.deliveryWindow||''} onChange={e=>set('deliveryWindow',e.target.value)}/></label><label>Payment method<select value={form.paymentMethod} onChange={e=>set('paymentMethod',e.target.value)}><option>Cash on delivery</option><option>Credit card</option><option>Bank transfer</option></select></label><label>Driver<input value={form.driver||''} onChange={e=>set('driver',e.target.value)}/></label><label>Delivery date<input type="date" value={form.date} onChange={e=>set('date',e.target.value)}/></label><label className="full">Items / product details<textarea value={form.products?.map(p=>`${p.name} x${p.quantity} ($${p.price})`).join(', ')||''} onChange={e=>setForm(f=>({...f,products:[{name:e.target.value,quantity:1,price:Number(f.total)||0}],items:1}))}/></label><label className="full">Notes<textarea value={form.notes||''} onChange={e=>set('notes',e.target.value)}/></label></div><div className="formactions"><button type="button" className="btn" onClick={()=>nav('/orders')}>Cancel</button><button className="btn primary">{editing?'Save changes':'Create order'}</button></div></form></div>}
/* 2: hna order details page b7ot full view w progress 3ashan tracking. */
