function App(){const[dark,setDark]=useState(()=>localStorage.getItem('routely-theme')==='dark'||(!localStorage.getItem('routely-theme')&&window.matchMedia('(prefers-color-scheme: dark)').matches));
const[mobile,setMobile]=useState(false);
const[collapsed,setCollapsed]=useState(()=>localStorage.getItem('routely-sidebar')==='collapsed');
const[panel,setPanel]=useState<'search'|'notifications'|'profile'|null>(null);
const[q,setQ]=useState('');
const[orders,setOrders]=useState<Order[]>([]);
const[user,setUser]=useState<AuthUser|null>(()=>authService.current());
useEffect(()=>{localStorage.setItem('routely-theme',dark?'dark':'light');
document.documentElement.classList.toggle('dark-mode',dark)},[dark]);
useEffect(()=>{localStorage.setItem('routely-sidebar',collapsed?'collapsed':'expanded')},[collapsed]);
useEffect(()=>{const close=(e:KeyboardEvent)=>{if(e.key==='Escape'){setMobile(false);
setPanel(null)}};window.addEventListener('keydown',close);
return()=>window.removeEventListener('keydown',close)},[]);
useEffect(()=>{orderService.list().then(x=>setOrders(x.length?x:seed())).catch(()=>setOrders(seed()))},[]);
if(!user)return <Routes><Route path="/register" element={<Register onLogin={setUser}/>}/><Route path="*" element={<Login onLogin={setUser}/>}/></Routes>;const logout=()=>{authService.logout();
setUser(null)};const userName=typeof user.name==='string'&&user.name?user.name:typeof user.email==='string'&&user.email?user.email:'User';return <div className={dark?'app dark':'app'}><Sidebar user={user} mobile={mobile} collapsed={collapsed} close={()=>setMobile(false)}/>{mobile&&<button className="sidebarOverlay" aria-label="Close navigation" onClick={()=>setMobile(false)}/>}<main><header><button className="icon mobile" aria-label="Open navigation" onClick={()=>setMobile(true)}><Menu/></button><button className="icon desktopSidebarToggle" aria-label={collapsed?'Expand sidebar':'Collapse sidebar'} onClick={()=>setCollapsed(v=>!v)}>{collapsed?<Menu/>:<X/>}</button><div><div className="eyebrow">LEBANON • OPERATIONS</div><h1>{pageTitle()}</h1></div><div className="actions"><button aria-label="Search" className="icon" onClick={()=>setPanel(panel==='search'?null:'search')}><Search/></button><button aria-label="Notifications" className="icon" onClick={()=>setPanel(panel==='notifications'?null:'notifications')}><Bell/><i/></button><button aria-label="Toggle dark mode" className="icon" onClick={()=>setDark(!dark)}>{dark?<Sun/>:<Moon/>}</button><button className="avatar" onClick={()=>setPanel(panel==='profile'?null:'profile')}>{userName.split(' ').map(part=>part[0]).join('').slice(0,2)}</button></div>{panel==='search'&&<div className="headerPanel searchPanel"><div className="search"><Search size={16}/><input autoFocus placeholder="Search orders, customers, tracking..." value={q} onChange={e=>setQ(e.target.value)}/></div>{q&&orders.filter(o=>Object.values(o).join(' ').toLowerCase().includes(q.toLowerCase())).slice(0,5).map(o=><Link key={o.id} to={'/orders/'+o.id} onClick={()=>setPanel(null)}><b>{o.id}</b><span>{o.customer} · {o.trackingNumber}</span></Link>)}</div>}{panel==='notifications'&&<div className="headerPanel"><b>Notifications</b><p>3 shipments need attention</p><p>Driver Fadi is near Beirut</p><Link to="/tracking" onClick={()=>setPanel(null)}>Open tracking</Link></div>}{panel==='profile'&&<div className="headerPanel"><b>{userName}</b><p>{user.role} · {user.city}</p><Link to="/profile" onClick={()=>setPanel(null)}>My profile</Link><button className="btn" onClick={logout}>Log out</button></div>}</header><Routes><Route path="/" element={<Dashboard/>}/><Route path="/orders" element={<Orders/>}/><Route path="/orders/new" element={<OrderForm/>}/><Route path="/orders/:id/edit" element={<OrderForm/>}/><Route path="/orders/:id" element={<OrderDetails/>}/><Route path="/tracking" element={<Tracking/>}/><Route path="/customers" element={<Customers/>}/><Route path="/analytics" element={<Analytics/>}/><Route path="/settings" element={<SettingsPage/>}/><Route path="/profile" element={<ProfilePage user={user} onUpdate={setUser} onLogout={logout}/>}/><Route path="*" element={<Navigate to="/" replace/>}/></Routes></main></div>}
/* 2: hna pageTitle b7ot route label 3ashan UX ykoun clear. */
function pageTitle(){const p=useLocation().pathname;return p==='/'?'Good morning':p.includes('tracking')?'Live tracking':p.includes('orders')?'Orders':p.includes('customers')?'Customers':p.includes('analytics')?'Analytics':'Settings'}
/* 5: hna sidebar b7ot nav w workspace info 3ashan users ykounu easy. */
function Sidebar({user,mobile,collapsed,close}:{user:AuthUser;mobile:boolean;collapsed:boolean;close:()=>void}){const pathname=useLocation().pathname;const nav=[['/','Overview',LayoutDashboard],['/orders','Orders',ShoppingCart],['/tracking','Tracking',Truck],['/customers','Customers',Users],['/analytics','Analytics',BarChart3]] as const;const identity=typeof user.name==='string'&&user.name?user.name:typeof user.email==='string'&&user.email?user.email:'User';const initials=identity.split(' ').map(part=>part[0]).join('').slice(0,2).toUpperCase();
return <aside className={(mobile?'open ':'')+(collapsed?'collapsed':'')}><div className="brand"><div className="logo"><Truck size={20}/></div><b>Route<span>ly</span></b><button className="icon close" aria-label="Close navigation" onClick={close}><X/></button></div><div className="workspace"><span className="wsdot"/> <span>Beirut Hub</span></div><nav>{nav.map(([to,label,I])=><Link title={label} onClick={close} className={pathname===to?'active':''} to={to} key={to}><I size={18}/><span>{label}</span></Link>)}</nav><div className="navbottom"><Link title="Settings" onClick={close} to="/settings"><Settings size={18}/><span>Settings</span></Link><div className="support">Need help?<small>Visit our support center</small></div><Link className="profile" title="Open profile" onClick={close} to="/profile"><div className="avatar">{initials}</div><b>{user.name||user.email||'User'}<small>{user.role||'User'}</small></b></Link></div></aside>}
/* 7: hna status badge b7ot visual feedback 3ashan order status yb2a clear. */
function Badge({status}:{status?:Status}){const normalized=safeStatus(status);
return <span className={'badge '+normalized.toLowerCase().replace(' ','-')}><i/>{normalized}</span>}
/* 3: hna order table b7ot list w actions 3ashan manage orders bser3a. */
function OrderTable({rows,onDelete,onEdit}:{rows:Order[];onDelete?:(id:string)=>void;onEdit?:(id:string)=>void}){return <div className="tablewrap"><table><thead><tr><th>Order</th><th>Customer</th><th>Destination</th><th>Tracking</th><th>Date</th><th>Amount</th><th>Status</th><th>Actions</th></tr></thead><tbody>{rows.map(o=><tr key={o.id}><td><Link className="orderid" to={'/orders/'+o.id}>{o.id}</Link></td><td>{o.customer||'Unknown'}<small className="subline">{o.phone||'No phone'}</small></td><td>{destinationFor(o)}</td><td>{o.trackingNumber||'—'}</td><td>{o.date||'—'}</td><td><b>${(o.total||0).toFixed(2)}</b></td><td><Badge status={o.status||'Processing'}/></td><td><button className="icon" aria-label={`Edit ${o.id}`} onClick={()=>onEdit?.(o.id)}><Edit3 size={15}/></button><button className="icon" aria-label={`Delete ${o.id}`} onClick={()=>onDelete?.(o.id)}><Trash2 size={15}/></button></td></tr>)}</tbody></table></div>}
/* 5: hna dashboard b7ot KPIs w chart 3ashan user yshouf performance fast. */
function Dashboard(){const[rows,setRows]=useState<Order[]>([]);
const nav=useNavigate();
useEffect(()=>{orderService.list().then(x=>setRows(x.length?x:seed())).catch(()=>setRows(seed()))},[]);
const active=rows.find(o=>o.status==='In Transit')||rows[0];return <div className="page"><div className="welcome"><div><p className="muted">{new Date().toLocaleDateString(undefined,{weekday:'long',year:'numeric',month:'long',day:'numeric'})}</p><h2>Your logistics at a glance.</h2></div><Link className="btn primary" to="/orders/new"><Plus size={17}/>Create order</Link></div><div className="stats"><Stat label="Total orders" value="1,284"/><Stat label="In transit" value="286"/><Stat label="Delivered" value="936"/><Stat label="Revenue" value="$48,290"/></div><div className="grid2"><section className="card chartcard"><h3>Orders overview</h3><p className="muted">Order volume for the last 7 days</p><ResponsiveContainer width="100%" height={240}><AreaChart data={[34,48,41,67,58,82,74].map((orders,i)=>({day:['Mon','Tue','Wed','Thu','Fri','Sat','Sun'][i],orders}))}><XAxis dataKey="day"/><YAxis/><Tooltip/><Area type="monotone" dataKey="orders" stroke="#635bff" fill="#635bff" fillOpacity={.15}/></AreaChart></ResponsiveContainer></section><MapCard order={active}/></div><section className="card tablecard"><div className="cardtitle"><h3>Recent orders</h3><Link to="/orders">View all orders <ArrowUpRight size={15}/></Link></div><OrderTable rows={[...rows].sort((a,b)=>new Date(b.updatedAt||b.createdAt||b.date).getTime()-new Date(a.updatedAt||a.createdAt||a.date).getTime()).slice(0,5)} onEdit={id=>nav('/orders/'+id+'/edit')}/></section></div>}
/* 2: hna stat card b7ot metrics 3ashan summary y7a9eq. */
function Stat({label,value}:{label:string;value:string}){return <div className="stat"><div className="stathead">{label}<Package size={18}/></div><strong>{value}</strong><small>? 12.5% <span>vs last month</span></small></div>}
/* 7: hna map card b7ot track vibe 3ashan route visuals yb2a easy. */
function MapCard({order}:{order?:Order}){return <section className="card"><div className="cardtitle"><h3>Live tracking</h3><Link to="/tracking">Open tracker</Link></div><LeafletTrackingMap order={order}/></section>}
/* 3: hna orders page b7ot search w CRUD 3ashan operations ykounu smooth. */
function Orders(){const[rows,setRows]=useState<Order[]>([]);
const[q,setQ]=useState('');
const nav=useNavigate();
const load=()=>orderService.list().then(x=>setRows(x.length?x:seed())).catch(()=>setRows(seed()));
useEffect(()=>{load()},[]);
const remove=async(id:string)=>{if(confirm('Delete this order?')){await orderService.remove(id);
setRows(r=>r.filter(x=>x.id!==id))}};return <div className="page"><div className="toolbar"><div><p className="muted">Manage and track every shipment</p><h2>All orders</h2></div><Link className="btn primary" to="/orders/new"><Plus/>New order</Link></div><div className="card tablecard"><div className="filters"><div className="search"><Search size={17}/><input placeholder="Search orders..." value={q} onChange={e=>setQ(e.target.value)}/></div></div><OrderTable rows={rows.filter(o=>Object.values(o).join(' ').toLowerCase().includes(q.toLowerCase()))} onDelete={remove} onEdit={id=>nav('/orders/'+id+'/edit')}/></div></div>}
/* 5: hna form b7ot create/edit order w save fallback 3ashan data keep stable. */
function OrderForm(){const nav=useNavigate();
const {id}=useParams();
const editing=Boolean(id&&id!=='new');
const[error,setError]=useState('');
const[original,setOriginal]=useState<Order|null>(null);
const[form,setForm]=useState<Partial<Order>>({customer:seedNames[0],customerId:'cust-001',city:'Beirut',address:'Hamra Street',phone:'+961 70 000000',total:50,status:'Processing',date:new Date().toISOString().slice(0,10),items:1,progress:20,trackingNumber:`LB${Date.now()}`,paymentMethod:'Cash on delivery',deliveryWindow:'10:00 - 14:00',notes:'',driver:'Unassigned',products:[{name:'Product',quantity:1,price:50}],createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()});
useEffect(()=>{if(editing)orderService.list().then(x=>{const found=x.find(o=>o.id===id);
if(found){setOriginal(found);
setForm(found)}else setError('Order not found.')}).catch(()=>setError('Could not load this order.'))},[editing,id]);
const set=(k:keyof Order,v:string)=>setForm(f=>({...f,[k]:k==='total'||k==='items'||k==='progress'?Number(v):k==='status'?safeStatus(v):v}));
return <div className="page formpage"><div className="toolbar"><div><p className="muted">Orders / {editing?'Edit':'New'} order</p><h2>{editing?'Edit order':'Create new order'}</h2></div></div><form className="card form" onSubmit={async e=>{e.preventDefault();
setError('');
try{const selectedIndex=Math.max(0,seedNames.indexOf(form.customer||''));
const payload={...form,customerId:form.customerId||`cust-${String(selectedIndex+1).padStart(3,'0')}`,status:safeStatus(form.status),...(editing&&original&&form.status!==original.status&&form.progress===original.progress?{progress:progressForStatus(safeStatus(form.status))}: {})};if(editing&&id)await orderService.update(id,payload);
else await orderService.create({id:`ORD-${Date.now()}`,...payload} as Order);
nav('/orders')}catch(err){setError(err instanceof Error?err.message:'Could not save order.')}}}><h3>Shipment details</h3>{error&&<p className="error">{error}</p>}<div className="formgrid"><label>Customer<select value={form.customer||''} onChange={e=>set('customer',e.target.value)}>{seedNames.map(n=><option key={n}>{n}</option>)}</select></label><label>Phone<input value={form.phone||''} onChange={e=>set('phone',e.target.value)}/></label><label className="full">Address<input value={form.address||''} onChange={e=>set('address',e.target.value)}/></label><label>City<input value={form.city||''} onChange={e=>set('city',e.target.value)}/></label><label>Status<select value={safeStatus(form.status)} onChange={e=>set('status',e.target.value)}>{statusOptions.map(status=><option key={status} value={status}>{status}</option>)}</select></label><label>Amount<input type="number" value={form.total||0} onChange={e=>set('total',e.target.value)}/></label><label>Tracking number<input value={form.trackingNumber||''} onChange={e=>set('trackingNumber',e.target.value)}/></label><label>Delivery window<input value={form.deliveryWindow||''} onChange={e=>set('deliveryWindow',e.target.value)}/></label><label>Payment method<select value={form.paymentMethod} onChange={e=>set('paymentMethod',e.target.value)}><option>Cash on delivery</option><option>Credit card</option><option>Bank transfer</option></select></label><label>Driver<input value={form.driver||''} onChange={e=>set('driver',e.target.value)}/></label><label>Delivery date<input type="date" value={form.date} onChange={e=>set('date',e.target.value)}/></label><label className="full">Items / product details<textarea value={form.products?.map(p=>`${p.name} x${p.quantity} ($${p.price})`).join(', ')||''} onChange={e=>setForm(f=>({...f,products:[{name:e.target.value,quantity:1,price:Number(f.total)||0}],items:1}))}/></label><label className="full">Notes<textarea value={form.notes||''} onChange={e=>set('notes',e.target.value)}/></label></div><div className="formactions"><button type="button" className="btn" onClick={()=>nav('/orders')}>Cancel</button><button className="btn primary">{editing?'Save changes':'Create order'}</button></div></form></div>}
/* 2: hna order details page b7ot full view w progress 3ashan tracking. */
function OrderDetails(){const id=useLocation().pathname.split('/').pop()!;const[order,setOrder]=useState<Order|null>(null);
useEffect(()=>{orderService.list().then(x=>setOrder(x.find(o=>o.id===id)||seed().find(o=>o.id===id)||null))},[id]);
const changeStatus=async(status:Status)=>{if(!order)return;const progress=order.progress===progressForStatus(safeStatus(order.status))?progressForStatus(status):order.progress;const updated=await orderService.update(order.id,{status,progress});
setOrder({...order,...updated,status,progress})};if(!order)return <div className="page"><h2>Order not found</h2></div>;return <div className="page"><Link className="back" to="/orders">← Back to orders</Link><div className="detailhead"><div><p className="muted">Order details · {order.trackingNumber}</p><h2>{order.id} <Badge status={order.status}/></h2></div><div className="formactions"><label>Status<select value={safeStatus(order.status)} onChange={e=>changeStatus(safeStatus(e.target.value))}>{statusOptions.map(status=><option key={status}>{status}</option>)}</select></label><Link className="btn primary" to={'/orders/'+id+'/edit'}>Edit order</Link></div></div><div className="grid2"><section className="card"><h3>Delivery progress</h3><div className="progress"><div style={{width:order.progress+'%'}}/></div><p>{order.progress}% complete · {cleanDestination(order.city)}</p><div className="detailinfo"><div><small>Recipient</small><b>{order.customer}</b><span>{order.phone}</span></div><div><small>Address</small><b>{cleanDestination(order.address)}</b><span>{order.deliveryWindow}</span></div><div><small>Payment</small><b>{order.paymentMethod}</b><span>Driver: {order.driver}</span></div><div><small>Products</small><b>{order.products?.map(p=>`${p.name} ×${p.quantity}`).join(', ')}</b><span>{order.notes||'No notes'}</span></div></div></section><MapCard order={order}/></div></div>}
/* 7: hna tracking section b7ot simulation updates 3ashan live view active. */
function Tracking(){const[rows,setRows]=useState<Order[]>([]);
const[selected,setSelected]=useState('');
const[running,setRunning]=useState(true);
useEffect(()=>{orderService.list().then(x=>{const data=x.length?x:seed();
setRows(data);
setSelected(data.find(o=>o.status==='In Transit')?.id||data[0]?.id||'')})},[]);
useEffect(()=>{if(!running)return;const timer=setInterval(()=>setRows(current=>current.map(o=>o.id===selected&&o.progress<100?{...o,progress:Math.min(100,o.progress+2),status:o.progress+2>=100?'Delivered':'In Transit'}:o)),1000);
return()=>clearInterval(timer)},[running,selected]);
const active=rows.find(o=>o.id===selected);
return <div className="page"><div className="toolbar"><div><p className="muted">Provider-isolated tracking simulator</p><h2>Live shipment tracking</h2></div><button className="btn" onClick={()=>setRunning(!running)}>{running?<Pause size={16}/>:<Play size={16}/>} {running?'Pause simulation':'Resume simulation'}</button></div><div className="grid2 trackinggrid"><section className="card shipmentlist"><div className="cardtitle"><div><h3>Active shipments</h3><p>Select a shipment to inspect its route</p></div><span className="count">{rows.filter(o=>o.status==='In Transit'||o.id===selected).length}</span></div><div className="trackingrows">{rows.filter(o=>o.status==='In Transit'||o.id===selected).slice(0,8).map(o=><button type="button" aria-pressed={o.id===selected} className={'trackingrow '+(o.id===selected?'selected':'')} key={o.id} onClick={()=>setSelected(o.id)}><span className="trackingidentity"><b>{o.id}</b><small>{cleanDestination(o.customer,o.city)}</small></span><span className="trackingstatus"><Badge status={o.status}/><strong>{o.progress}%</strong></span><span className="trackingprogress" aria-hidden="true"><i style={{width:`${o.progress}%`}}/></span></button>)}</div></section><section className="card trackingdetail"><h3>{active?.id||'Select a shipment'}</h3><p className="muted">{active?cleanDestination(active.customer,active.city,'Lebanon'):'Choose an active shipment to view its route'}</p><LeafletTrackingMap order={active}/></section></div></div>}
/* 3: hna customers page b7ot CRUD w search 3ashan manage clients. */
function Customers(){const[rows,setRows]=useState<Customer[]>([]);
const[orders,setOrders]=useState<Order[]>([]);
const[name,setName]=useState('');
const[selected,setSelected]=useState<string|null>(null);
useEffect(()=>{Promise.all([customerService.list(),orderService.list()]).then(([customers,loadedOrders])=>{const data=customers.length?customers:seedNames.map((name,i)=>safeCustomer({id:`cust-${String(i+1).padStart(3,'0')}`,name,email:`${name.toLowerCase().replace(/ /g,'_')}@example.com`,phone:`+961 70 ${120000+i}`,address:`${10+i} Main Street`,city:cities[i%7],joinedDate:'2025-01-15'},i));
setRows(data.map(safeCustomer));
setOrders(loadedOrders.length?loadedOrders:seed())}).catch(()=>setOrders(seed()))},[]);
const stats=(c:Customer)=>{const matches=orders.filter(o=>(o.customerId&&o.customerId===c.id)||(o.customerEmail&&o.customerEmail.toLowerCase()===c.email.toLowerCase())||(!o.customerId&&!o.customerEmail&&o.customer===c.name));
const latest=[...matches].sort((a,b)=>new Date(b.date||b.updatedAt).getTime()-new Date(a.date||a.updatedAt).getTime())[0];return {matches,count:matches.length,total:matches.reduce((sum,o)=>sum+(Number(o.total)||0),0),last:latest?.date||'—'}};const add=async()=>{if(!name.trim())return;const c=safeCustomer({id:crypto.randomUUID(),name:name.trim(),fullName:name.trim(),city:'Beirut',joinedDate:new Date().toISOString().slice(0,10)},rows.length);
await customerService.create(c);
setRows(r=>[c,...r]);
setName('')};const edit=async(c:Customer)=>{const n=prompt('Customer full name',c.fullName||c.name);
if(n&&n!==c.fullName){await customerService.update(c.id,{name:n,fullName:n});
setRows(r=>r.map(x=>x.id===c.id?{...x,name:n,fullName:n}:x))}};const remove=async(id:string)=>{if(confirm('Delete customer?')){await customerService.remove(id);
setRows(r=>r.filter(x=>x.id!==id));
if(selected===id)setSelected(null)}};return <div className="page"><div className="toolbar"><div><p className="muted">Customer profiles and order history</p><h2>Customers</h2></div><div className="search"><input placeholder="New customer name" value={name} onChange={e=>setName(e.target.value)}/><button className="btn primary" onClick={add}><Plus/>Add</button></div></div><div className="customergrid">{rows.map(c=>{const s=stats(c);
const active=c.status!=='Inactive'&&s.count>0;return <div className="card customerCard" key={c.id}><div className="customerTop"><div className="bigavatar">{c.name.split(' ').map(x=>x[0]).join('').slice(0,2)}</div><div><button className="icon" aria-label={`Edit ${c.name}`} onClick={()=>edit(c)}><Edit3 size={15}/></button><button className="icon" aria-label={`Delete ${c.name}`} onClick={()=>remove(c.id)}><Trash2 size={15}/></button></div></div><h3>{c.fullName||c.name} <span className="count">{active?'Active':c.status}</span></h3><p>{c.email||'No email'} · {c.phone||'No phone'}</p><p>{c.address||'No address'}{c.area?`, ${c.area}`:''}, {c.city}</p><div className="customerMeta"><span>{s.count} orders</span><b>${s.total.toFixed(2)}</b><span>Last: {s.last}</span></div><button className="btn" onClick={()=>setSelected(selected===c.id?null:c.id)}>{selected===c.id?'Hide history':'View order history'}</button>{selected===c.id&&<div className="customerHistory">{s.matches.length?s.matches.map(order=><Link key={order.id} to={`/orders/${order.id}`}><b>{order.id}</b><span>{order.date||'—'} · ${(order.total||0).toFixed(2)}</span><Badge status={order.status}/></Link>):<small>No orders linked yet.</small>}</div>}</div>})}</div></div>}
/* 5: hna analytics page b7ot charts 3ashan insights w performance summary. */
function Analytics(){return <div className="page"><div className="toolbar"><div><p className="muted">Performance insights</p><h2>Analytics</h2></div></div><div className="stats"><Stat label="On-time delivery" value="94.8%"/><Stat label="Avg. delivery time" value="2.4 days"/><Stat label="Return rate" value="1.8%"/><Stat label="Active customers" value="248"/></div><section className="card chartcard"><h3>Delivery performance</h3><ResponsiveContainer width="100%" height={320}><AreaChart data={[34,48,41,67,58,82,74].map((orders,i)=>({day:i+1,orders}))}><XAxis dataKey="day"/><YAxis/><Tooltip/><Area type="monotone" dataKey="orders" stroke="#14b89a" fill="#14b89a" fillOpacity={.15}/></AreaChart></ResponsiveContainer></section></div>}
/* 2: hna settings page b7ot preferences 3ashan workspace personalized. */
function SettingsPage(){return <div className="page"><div className="toolbar"><div><p className="muted">Workspace preferences</p><h2>Settings</h2></div></div><section className="card form"><h3>Company profile</h3><div className="formgrid"><label>Company name<input defaultValue="RouteLy Logistics"/></label><label>Contact email<input defaultValue="hello@routely.lb"/></label></div><div className="formactions"><button type="button" className="btn primary">Save changes</button></div></section></div>}
/* 4: hna login/register screens b7ot feedback wadih w support lal user aw admin. */
function Login({onLogin}:{onLogin:(user:AuthUser)=>void}){const[email,setEmail]=useState('');
const[password,setPassword]=useState('');
const[error,setError]=useState('');
const submit=(event:React.FormEvent)=>{event.preventDefault();
try{onLogin(authService.login(email,password))}catch(err){setError(err instanceof Error?err.message:'Could not sign in.')}};return <AuthShell title="Welcome back" subtitle="Sign in to manage your deliveries"><form className="card form" onSubmit={submit}>{error&&<p className="error">{error}</p>}<label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="btn primary">Sign in</button><p className="muted">New admin? <Link to="/register">Create an admin account</Link></p></form></AuthShell>}
function Register({onLogin}:{onLogin:(user:AuthUser)=>void}){const[form,setForm]=useState({name:'',email:'',password:'',phone:'',city:'Beirut',jobTitle:'Operations manager'});
const[error,setError]=useState('');
const set=(key:keyof typeof form,value:string)=>setForm(current=>({...current,[key]:value}));
const submit=async(event:React.FormEvent)=>{event.preventDefault();
try{onLogin(await authService.register(form))}catch(err){setError(err instanceof Error?err.message:'Could not create account.')}};return <AuthShell title="Create admin account" subtitle="Set up your RouteLy workspace"><form className="card form" onSubmit={submit}>{error&&<p className="error">{error}</p>}<label>Full name<input required value={form.name} onChange={e=>set('name',e.target.value)}/></label><label>Email<input type="email" required value={form.email} onChange={e=>set('email',e.target.value)}/></label><label>Password<input type="password" minLength={6} required value={form.password} onChange={e=>set('password',e.target.value)}/></label><label>Phone<input value={form.phone} onChange={e=>set('phone',e.target.value)}/></label><label>City<input value={form.city} onChange={e=>set('city',e.target.value)}/></label><button className="btn primary">Register as admin</button><p className="muted">Already registered? <Link to="/">Sign in</Link></p></form></AuthShell>}
function AuthShell({title,subtitle,children}:{title:string;subtitle:string;children:React.ReactNode}){return <div className="authpage"><div className="authbrand"><div className="logo"><Truck size={20}/></div><b>Route<span>ly</span></b></div><h1>{title}</h1><p className="muted">{subtitle}</p>{children}</div>}
function ProfilePage({user,onUpdate,onLogout}:{user:AuthUser;onUpdate:(user:AuthUser)=>void;onLogout:()=>void}){const[form,setForm]=useState<ProfileUpdate>({name:user.name,email:user.email,phone:user.phone,city:user.city,jobTitle:user.jobTitle});
const[saved,setSaved]=useState(false);
const[error,setError]=useState('');
const set=(key:keyof ProfileUpdate,value:string)=>setForm(current=>({...current,[key]:value}));
const save=()=>{try{onUpdate(authService.updateProfile(user.id,form));
setSaved(true);
setError('')}catch(err){setError(err instanceof Error?err.message:'Could not update profile.')}};return <div className="page formpage"><div className="toolbar"><div><p className="muted">Account</p><h2>My profile</h2></div><button className="btn" onClick={onLogout}>Log out</button></div><section className="card form"><h3>Personal details</h3>{error&&<p className="error">{error}</p>}{saved&&<p className="success">Profile saved successfully.</p>}<div className="formgrid"><label>Full name<input value={form.name} onChange={e=>set('name',e.target.value)}/></label><label>Email<input type="email" value={form.email} onChange={e=>set('email',e.target.value)}/></label><label>Phone<input value={form.phone} onChange={e=>set('phone',e.target.value)}/></label><label>City<input value={form.city} onChange={e=>set('city',e.target.value)}/></label><label className="full">Job title<input value={form.jobTitle} onChange={e=>set('jobTitle',e.target.value)}/></label></div><div className="formactions"><button type="button" className="btn primary" onClick={save}>Save profile</button></div></section></div>}
/* 7: hna render boot b7ot app root 3ashan router ybda l-navigation. */
createRoot(document.getElementById('root')!).render(<BrowserRouter><App/></BrowserRouter>);

